# GM-030 Streamlit Community Cloud runtime requirements.
# Exactly the bounded runtime/UI dependencies the hosted app needs — the
# project's runtime dependencies (pyproject.toml [project].dependencies) plus
# the UI dependency (the [ui] extra). Development-only tools (pytest,
# hypothesis, ruff, mypy, pre-commit) are deliberately absent: they install
# via `pip install -e ".[dev,ui]"` for local work, never on the host.
streamlit>=1.32,<2
PyYAML>=6,<7
pydantic>=2,<3
tzdata>=2024.1
.
