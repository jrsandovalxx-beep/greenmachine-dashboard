# GM-040-HF1 Streamlit Community Cloud runtime requirements.
# Install the local GreenMachine project so importlib.metadata can resolve
# the greenmachine distribution version on the host.
-e .

streamlit>=1.32,<2
PyYAML>=6,<7
pydantic>=2,<3
tzdata>=2024.1
